<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
 <mapper namespace="club.nextcoder.yunnote.dao.UserDao">
 	<!-- 按用户名查找数据 -->
 	<select id="findByName" parameterType="string" resultType="club.nextcoder.yunnote.entity.User">
 		select * from cn_user where cn_user_name=#{name}
 	</select>
 	<!-- 新增数据 -->
 	<insert id="saveUser" parameterType="club.nextcoder.yunnote.entity.User">
 		insert into cn_user 
 		(cn_user_id,cn_user_name,cn_user_password,cn_user_token,cn_user_nick) 
 		values 
 		(#{cn_user_id},#{cn_user_name},#{cn_user_password},#{cn_user_token},#{cn_user_nick})
 	</insert>
 </mapper>